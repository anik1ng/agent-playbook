dist
build
coverage

# Lockfiles are their package manager's format, not Prettier's. Reformatting
# one produces a diff every install undoes.
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb
